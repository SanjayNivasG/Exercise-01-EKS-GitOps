apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: payment-service
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80
